print("Hi")

# String functions: 
# 1) .strip() removes any white spaces 
# 2) .split(", ") Splits a string into a array 
#  into a list of subtrings based on delimtere in our case a ", "
# EXP: 
# s = "Hello, World"
# print(s.split(", "))  # Output: ['Hello', 'World']
# 3) (", ").join(words) joins elements of an iterable (like list) into a single string
# 4) .startswith("word_lookingfor") check if string starts with this
# 5) .find("word_lookingfor") returns the index of first occurrence returns -1 if not found
# 6) .index("word_lookingfor") raises an exception if not found
# 7) .replace("word_being_replaced, word_being_replced_with")

